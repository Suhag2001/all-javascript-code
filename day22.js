//*Navigation by using javascript

//   window.history.back(); //it is use for back the page

//   window.history.forward(); //it will forward the page

//*window location

// location.reload(); // it will reload the page

// location.href = "http://www.google.com";
//it will redirect us on the given link like anchor tag ..

//window.navigator.geolocation.getCurrentPosition
//it will give the only one time latitude and longitude

// window.navigator.geolocation.watchPosition
//It will give the continue latitude and longitude

//web storage

// Web storages store data in key value pair

// localStorage :- store data for long period .

// sessionStorage :- store the data till current window (tab) is open.

// setItem(key, value):- save a key value pair .
//key must be unique if same key exists then it will replace existing value .

// getItem(key) : returns the value associated with the key.

// removeItem(key):- removes the key value pair associated with the key.

// clear():- removes all the key value pairs for the current domain.
